// import React from 'react'
// import axios from 'axios';

function Upload() {
    // const [file, setFile] = React.useState(null)
    // const imageRef = React.useRef();
    // const uploadFile = async (e: any) => {
    //     setFile(e.target.files[0])
    //     console.log("File: ", file, imageRef.current?.files[0], imageRef.current?.files[0]?.size + "bytes")
    //     if(imageRef.current?.files[0]){
    //         axios.post("http://localhost:9999/images", imageRef.current?.files[0]).then(res => {
    //         console.log("RES: ", res)
    //         setFile(null)
    //     })
    //     } 
    // }
  return (
    <div>
        {/* <input type="file" onChange={uploadFile} ref = {imageRef}/> */}
    </div>
  )
}

export default Upload