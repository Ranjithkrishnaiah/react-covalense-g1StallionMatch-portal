import { styled } from '@mui/material/styles';
import { AccordionActions } from '@mui/material';
const CustomAccordionActions = styled(AccordionActions)(() => ({
    padding:'0px',

}))

export default CustomAccordionActions