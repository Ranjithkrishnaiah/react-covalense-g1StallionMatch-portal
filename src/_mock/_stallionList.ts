//import _mock from '../_mock';

export const _stallionCards = [...Array(20)].map((_, index) => ({
 //name: _mock.name.fullName(index), 
  name: "Adelaide (IRE)",
  year: "2015",
  description : 'Coolmore Stud NSW',
  amount : 2500,
  amountYear : '2018',
  //imageUrl: _mock.image.stallion(index)
}));
